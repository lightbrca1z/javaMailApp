# メール送信設定ガイド

## 📧 現在の問題

メールが送信されない場合、以下の原因が考えられます：

### 1. **Gmailアプリパスワードが必要**
Googleアカウントの通常のパスワードでは、セキュリティ上の理由でメール送信ができません。
**Googleアプリパスワード**を取得する必要があります。

### 2. **2段階認証が必要**
アプリパスワードを生成するには、Googleアカウントで2段階認証が有効になっている必要があります。

---

## 🔧 メール送信を有効にする手順

### ステップ1: Googleアカウントで2段階認証を有効化

1. **Googleアカウントにアクセス**
   - https://myaccount.google.com/ にアクセス
   - ログインする

2. **セキュリティ設定を開く**
   - 左メニューから「セキュリティ」をクリック

3. **2段階認証プロセスを有効化**
   - 「2段階認証プロセス」をクリック
   - 指示に従って設定を完了

### ステップ2: アプリパスワードを生成

1. **アプリパスワード設定にアクセス**
   - 直接アクセス: https://myaccount.google.com/apppasswords
   - または、Googleアカウント → セキュリティ → 2段階認証プロセス → アプリパスワード

2. **アプリパスワードを生成**
   - 「アプリを選択」→「その他（カスタム名）」を選択
   - 名前を入力: 例）「MailSendServlet」
   - 「生成」ボタンをクリック

3. **生成されたパスワードをコピー**
   - 16桁のパスワードが表示されます（スペースは無視してOK）
   - 例: `abcd efgh ijkl mnop` → `abcdefghijklmnop`
   - このパスワードをコピーしてください

### ステップ3: config.propertiesファイルを更新

1. **ファイルを開く**
   ```
   src/main/webapp/WEB-INF/config.properties
   ```

2. **メール設定を更新**
   ```properties
   # メール認証情報
   MAIL_USERNAME=quwieorpt@gmail.com
   MAIL_PASSWORD=生成したアプリパスワード
   ```

   **重要ポイント:**
   - `MAIL_USERNAME`は完全なメールアドレス（`@gmail.com`を含む）を入力
   - `MAIL_PASSWORD`はGoogleアカウントの通常パスワードではなく、上記で生成した**アプリパスワード**を入力

### ステップ4: サーバーを再起動

1. **Tomcatサーバーを停止**
2. **Eclipseでプロジェクトをクリーン**
   ```
   プロジェクト右クリック → Clean...
   ```
3. **Tomcatサーバーを再起動**

---

## ✅ 動作確認

### 期待されるログ

メール送信が正常に動作すると、以下のログが表示されます：

```
SMTP_HOST: smtp.gmail.com
SMTP_PORT: 587
MAIL_USERNAME: quwieorpt@gmail.com
📝 ContactServlet: doPost呼び出し - action=send
✅ 送信者アドレス設定: quwieorpt@gmail.com
✅ メールオブジェクトの作成が成功しました
📊 データベースへの保存が成功しました
📧 メール送信を開始します...
✅ メール送信が成功しました
```

### エラーが出る場合

#### エラー1: Authentication failed (認証失敗)
```
MessagingException: Authentication failed
```

**原因:**
- アプリパスワードが間違っている
- または、通常のGoogleアカウントパスワードを使用している

**解決方法:**
- アプリパスワードを再生成して、config.propertiesに正しく設定

#### エラー2: Invalid Addresses (無効なアドレス)
```
AddressException: Invalid Addresses
```

**原因:**
- メールアドレスの形式が間違っている
- `@gmail.com`が抜けている

**解決方法:**
- `MAIL_USERNAME=quwieorpt@gmail.com` のように完全なアドレスを設定

#### エラー3: Connection timed out (接続タイムアウト)
```
MessagingException: Connection timed out
```

**原因:**
- インターネット接続の問題
- ファイアウォールがポート587をブロックしている

**解決方法:**
- インターネット接続を確認
- ファイアウォール設定を確認

---

## 🧪 テストモード（メール送信なし）

メール送信を設定せずにテストしたい場合：

```properties
# メール認証情報（空にするとテストモード）
MAIL_USERNAME=
MAIL_PASSWORD=
```

この場合、以下のログが表示されます：
```
⚠️ メール設定が未設定のため、メール送信をスキップします
📊 データベースへの保存のみ完了しました
```

データベースへの保存は正常に動作し、メール送信のみがスキップされます。

---

## 📊 現在の設定状態

### config.properties の現在の設定:

```properties
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
MAIL_USERNAME=quwieorpt@gmail.com
MAIL_PASSWORD=Keeper2020!?!?!
```

### ⚠️ 注意

現在設定されているパスワード `Keeper2020!?!?!` が**Googleアカウントの通常パスワード**の場合、
メール送信は失敗します。

**必ず上記の手順でアプリパスワードを生成して設定してください。**

---

## 🔐 セキュリティに関する注意

### 1. パスワードをソースコードに書かない

❌ 悪い例（ContactServlet.javaに直接書く）:
```java
username = config.getProperty("MAIL_USERNAME", "quwieorpt@gmail.com");
password = config.getProperty("MAIL_PASSWORD", "mypassword");
```

✅ 良い例（config.propertiesに書く）:
```java
username = config.getProperty("MAIL_USERNAME", "");
password = config.getProperty("MAIL_PASSWORD", "");
```

### 2. config.propertiesをGit管理から除外

`.gitignore`に追加:
```
src/main/webapp/WEB-INF/config.properties
```

### 3. 本番環境では環境変数を使用

本番環境では、config.propertiesではなく環境変数からパスワードを読み込むことを推奨します。

---

## 📞 トラブルシューティング

### Q: アプリパスワードの生成画面が見つからない

A: 以下を確認してください：
1. Googleアカウントで2段階認証が有効になっているか
2. 直接URLにアクセス: https://myaccount.google.com/apppasswords
3. それでも見つからない場合、Googleアカウントの種類によっては利用できない場合があります

### Q: メールは送信されるが、受信トレイに届かない

A: 以下を確認してください：
1. 迷惑メールフォルダを確認
2. 送信先のメールアドレスが正しいか確認
3. Gmailの送信済みメールを確認

### Q: 複数のGmailアカウントで使用できますか？

A: はい、できます。config.propertiesのメールアドレスとアプリパスワードを変更するだけです。

---

## 📝 まとめ

メール送信を有効にするために必要なこと：

1. ✅ Googleアカウントで2段階認証を有効化
2. ✅ アプリパスワードを生成
3. ✅ config.propertiesに設定
   - `MAIL_USERNAME`: 完全なメールアドレス（@gmail.com付き）
   - `MAIL_PASSWORD`: 生成したアプリパスワード（16桁）
4. ✅ サーバーを再起動
5. ✅ ログで動作確認

**現在の状態:**
- メールアドレス: ✅ `quwieorpt@gmail.com` （正しい形式）
- パスワード: ⚠️ アプリパスワードの確認が必要

アプリパスワードを正しく設定すれば、メール送信が動作します！

